<div class="row" id="opening-carousel-wrap" >
	<div class="owl-carousel opening-carousel owl-theme">
	<% @posts.each do |post| %>
		<div>
			<%= link_to show_post_path(post) do %>
            <div class="carousel-item">
                <div class="carousel-title">
                    <h1 class="highlight">
                        <span class="color-category<%= post.category_id %>"><%= post.title %></span>
                    </h1>
                    <p class="carousel-post-date highlight"><span class="color-category<%= post.category_id %>"><%= post.created_at.to_date %></span></p>
                </div>
                <div class="opening-carousel-img" style="background-image:url('<%= post.image.contentimg.url %>');">
                </div>
            </div>
            <% end %>
		</div>
	<% end %>
	</div>
</div>

